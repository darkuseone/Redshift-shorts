# REDSHIFT — главный конфиг пайплайна.
# Эталон — Приложение A ТЗ v2.0; здесь он расширен полями, без которых
# пайплайн не собирается (провайдеры, пути, рендер, ретраи).
# Ни один секрет здесь не хранится: только имена переменных окружения.

project:
  name: redshift
  language: ru
  fps: 30
  resolution: [1080, 1920]

# --- Режим работы внешних сервисов -------------------------------------------
# live  — всегда реальные API (нет ключа → ошибка)
# mock  — всегда детерминированные заглушки (CI, разработка, тесты)
# auto  — live при наличии ключа в окружении, иначе mock (с warning в лог)
providers:
  mode: auto
  allow_xai: false          # IMAGE gen: xAI не в цепочке картинок. Vision = Grok.
  request_timeout_sec: 60
  retries: 3               # §10.5.3: 3 попытки (обычные ошибки)
  backoff_base_sec: 2.0    # экспоненциальный бэкофф 2/4/8
  capacity_retries: 6      # 503/UNAVAILABLE/high demand
  capacity_backoff_base_sec: 5.0  # 5/10/20/40/80

heygen:
  # Аватар 5 заказчика, выдан 24.08. Выданный id — это **группа** («Markus
  # Brown», 78 луков): `get_avatar_look` его не знает, а создание видео просит
  # id лука. Держим оба — группу как то, что дал заказчик, лук как то, что
  # уходит в запрос.
  avatar_group_id: "d0573be250724c23bed61037f1c7490d"
  # Лук «Markus Brown at the microphone». Выбран заказчиком взамен
  # `99ccc74e…`, и причина у замены техническая, а не вкусовая: прежний лук
  # приходил **ландшафтным** 1920×1080, и вертикаль из него добывалась кропом
  # (`tools/reframe_avatar.py`) — кадр сначала обрезали, потом тянули, и
  # качество терялось дважды. Этот лук снят вертикально (исходник 1536×2752),
  # кропать нечего.
  #
  # Id лука живёт ЗДЕСЬ и только здесь. До этой правки он был переписан в
  # шести местах — CLAUDE.md, instruction.md, два README, тест и ТЗ, — и когда
  # заказчик сменил лук, устарели все шесть разом: ролик собрался на прежнем
  # аватаре, потому что правило лежало в документации, а не в конфиге.
  # Документы теперь ссылаются сюда, а тест читает значение, а не повторяет.
  #
  # Движок — только Avatar V (`avatar_v`), как просил заказчик.
  avatar_id: "cfcb357563454e8cb7083258e4424992"
  # Look id только отсюда, не из секрета HEYGEN_AVATAR_ID (402 на live API).
  avatar_id_env: ""
  # Движок Avatar V — иначе API молча уходит в Avatar IV.
  engine: "avatar_v"
  model_version: "avatar5"                         # §7.4: вынесено в конфиг, не хардкод
  # prepared — клипы из MCP в assets/avatar_clips/<id>/. api в Actions не звать
  # (HEYGEN_API даёт 402). auto оставлен для тестов двухфазного выбора.
  source: prepared
  prepared_dir: assets/avatar_clips
  # Ключевание не нужно: аватар 5 отдаёт настоящую прозрачность. Проверено
  # измерением, а не на слово — 25.08, клип 1080x1920, `output_format: webm`:
  # 47 % кадра полностью прозрачны, 46 % полностью непрозрачны, край мягкий
  # (волосы, дужки очков, штанга микрофона вырезаны чисто).
  #
  # Прошлый лук того же человека (54172d7a…) прозрачности не отдавал ни в
  # mp4, ни в webm, и провайдер верил расширению файла — из-за этого приёмы за
  # головой уходили за непрозрачный план. Теперь альфа измеряется на каждом
  # клипе (`ffmpeg.has_alpha`), а расширение ничего не решает.
  #
  # Тонкость декодера: VP9 держит альфу отдельным блоком контейнера, штатный
  # декодер ffmpeg отдаёт по такому файлу yuv420p — «альфы будто нет».
  # Достаёт её только libvpx-vp9.
  prepared_chroma: ""
  background: "transparent_if_available"
  # 720p, не 1080p — решение заказчика. Дешевле у HeyGen, и это не тот
  # компромисс с качеством, которым сперва показался: у прежнего лука лицо
  # занимало 216 px из 1920 (11 % кадра, ландшафтный кроп растягивался в
  # вертикаль), у этого — около 290 px из 1280 (23 %). На лицо приходится
  # больше пикселей, чем было на 1080p прежнего лука, не меньше.
  output: "720x1280"
  # r63: заказчик — «растягивать видео из HeyGen не надо, как раньше».
  # 2.7 (и даже эффективные ~1.85 после MUST-009) значило одно и то же:
  # `_avatar_zoom_css` рисует уже закодированный VP9-кадр через CSS
  # width/height крупнее, чем он есть — браузер домысливает пиксели, которых
  # в файле нет. Прежний лук садился в кадр мелко и без этого лицо было не
  # разглядеть; этот лук снят вертикально и уже заполняет кадр — зумить
  # больше нечего, разве что портить.
  #
  # 1.0 — не «зум выключен», а другой режим целиком: `fit_compose_zoom`
  # видит requested_zoom<=1.0 и уходит в `_fit_natural` (без band-fit).
  # Клип рисуется 1:1, ни один пиксель не увеличивается; голова только
  # сдвигается по вертикали так, чтобы её измеренный верх (по альфе, не
  # догадкой) встал на `avatar.head_top_frac` кадра — заказчик прислал
  # референс, прикинутый в фоторедакторе, ровно под это число. Всё ниже
  # видимой части — стол, пол — просто уходит за overflow:hidden кадра.
  #
  # Значение выше 1.0 по-прежнему берёт старый band-fit (MUST-009,
  # `avatar.face_band_y`) — для лука, который в своём кадре сидит мелко.
  # Этому лук не подходит; трогать заново, только если лук сменится опять.
  compose_zoom: 1.0
  max_seconds_per_video: 42
  api_key_env: HEYGEN_API
  api_base: "https://api.heygen.com"
  poll_interval_sec: 10
  poll_timeout_sec: 900

elevenlabs:
  voice_id_env: ELEVENLABS_VOICE_ID    # клон голоса заказчика
  voice_id: ""                          # можно задать явно; пусто → берётся из env
  # Два клона одного голоса, почти одинаковые. Заказчик разрешил чередовать их
  # между роликами, чтобы канал не звучал одной дорожкой. Выбор идёт по
  # video_id, а не случайно: пересборка того же ролика обязана дать тот же
  # голос, иначе она переозвучит его заново — за деньги и с новыми границами
  # фраз, от которых бракуются уже снятые клипы ведущего.
  voice_pool:
    - "14NozJq5eoBmDc1FXFDq"
    - "7fU3YUxRrVGjNaZ5dzEH"
  model: "eleven_v3"
  fallback_model: "eleven_multilingual_v2"   # R-4
  # Формат ответа. Зависит от тарифа, а не от кода: pcm_* доступен не везде, и
  # на этом ключе PCM недоступен: на запрос pcm_44100 приходит mp3. Замерено
  # пробой голоса — прогоны 33571714855 и 33572242247, шесть замеров:
  #   eleven_v3            pcm_44100 8994 | mp3_192 9636 | mp3_128 8004
  #   eleven_multilingual  pcm_44100 8979 | mp3_192 5856
  # Отсюда две вещи. Первая: mp3_44100_192 — лучший доступный, он и стоит.
  # Вторая: потолок ~9.6 кГц один на все форматы и обе модели, а 192 кбит/с
  # несут около 19 кГц — значит режет не транспорт и не тариф, а сам голос.
  # Клон повторяет полосу записи, с которой снят; поднять её можно только
  # переснятым образцом и новым клоном.
  output_format: "mp3_44100_192"
  # Ниже этой полосы прогон пишет предупреждение: значит, упёрлись в тариф.
  min_bandwidth_hz: 14000
  # Характер подачи. Подбирается на слух, поэтому живёт здесь, а не в коде.
  # stability — ровность чтения: ниже значит шире интонационный размах.
  # На 0.45 разброс громкости готового ролика был 2.0 LU — ровная линия.
  voice_settings:
    stability: 0.30
    similarity_boost: 0.85
    style: 0.45
    use_speaker_boost: true
  # Темп речи ведущего относительно базового. 1.2 — TZ-0050 заказчик: так
  # просил заказчик. Вместе со скоростью на ту же долю сдвигается плановая
  # длительность, иначе коррекция длины вернула бы прежний темп.
  pace: 1.2
  length_buffer_pct: 22                      # §4.2.4: запас +18…25%
  api_key_env: ELEVENLABS_API_KEY
  api_base: "https://api.elevenlabs.io"
  sample_rate: 48000
  # SFX-генерация (только пока библиотека не заполнена до лимита §14.1)
  sfx_model: "eleven_text_to_sound_v2"

vision:
  # Critic / P8 / P12 semantic QC. Live Grok/Gemini выключены: заказчик
  # смотрит ролик сам. skip_live=false в заявке — единственный способ
  # снова вызвать vision API. Generation по-прежнему providers.allow_xai=false.
  primary: "grok"
  arbiter: "grok"
  fallback: "gemini"
  arbiter_max_calls: 3        # MUST-019: ≤3 на ролик, не 8
  accept_threshold: 0.70
  reject_threshold: 0.45
  frame_disagreement_threshold: 0.30
  grey_xai: true              # Grok как primary и как arbiter серой зоны
  grok_api_key_env: XAI_API_KEY
  grok_model: "grok-4.6"      # не grok-4-fast / grok-2-vision
  grok_api_base: "https://api.x.ai"
  gemini_api_key_env: GEMINI_API_KEY
  gemini_model: "gemini-3.8-flash"
  # true = без live vision даже при XAI_API_KEY / GEMINI_API_KEY.
  # P8: кэш/ранг/движковые гейты. P12: qc_skipped_semantic, файл выдаётся.
  # Смысловой QC — глаз заказчика. Mock-судья — только providers.mode=mock.
  skip_live: true
  # Запасной Flash только при стойком 503 на primary. 3.7 — предыдущий GA Flash
  # (релизы 3.7→3.8 за ~3 недели); 2.5 для новых ключей уже недоступен.
  gemini_model_fallback: "gemini-3.7-flash"
  gemini_api_base: "https://generativelanguage.googleapis.com"

generation:
  # true = без image gen (Gemini/Grok/Magnific): P9 + P12 thumbs → ffmpeg
  skip: false
  # Gemini-only image gen. Empty fallback: no xAI, Magnific HTTP still 404.
  source: gemini
  fallback: ""
  gemini_image_model: "gemini-3.1-flash-image"
  gemini_image_model_fallback: "gemini-2.5-flash-image"
  gemini_image_aspect: "9:16"
  # Слаг снят с живого ответа API: grok-2-image-1212 выведен из обращения
  # 2026-02-24, и сам сервис в тексте ошибки назвал замену. Ради этого модель и
  # вынесена в конфиг — устаревший слаг чинится строкой YAML, а не кодом.
  #
  # Заказчик просит самую качественную модель Imagine. Режим качества сервис
  # берёт полем запроса, а не отдельным слагом, поэтому он вынесен рядом:
  # `grok_image_params` подмешивается в тело запроса как есть. Если сервис
  # поле не примет, прогон не встанет — он перейдёт на запасную модель и
  # скажет об этом в логе, а поле убирается строкой YAML.
  grok_image_model: "grok-imagine-image"
  grok_image_model_fallback: "grok-2-image-1212"
  grok_image_params:
    quality: "high"
    aspect_ratio: "9:16"
    resolution: "2k"
  # Видео Grok/Gemini image не генерируют: кадр оживляется наездом (§H7 — всё
  # приближается), тем же приёмом Ken Burns, что и фотослоты стока.
  ken_burns_zoom: 1.12

magnific:
  max_downloads_per_video: 80     # raised so P7 force-rebuild can stock avatar BGs
  prefer_free_models: true
  paid_model_share_limit: 0.07
  api_key_env: MAGNIFIC_API_KEY
  api_base: "https://api.magnific.ai"
  # Слаги моделей и реальные цены — config/magnific_models.json, снят через
  # MCP. Безлимит подписки на API не распространяется, поэтому «free» здесь
  # значит «дешёвая»: разброс цен между моделями доходит до ста раз
  # (Classic Fast — 1 кредит, seedance-mini — 700 за пять секунд).
  catalog_file: config/magnific_models.json
  models:
    free_image:  "fast"              # 1 кредит
    paid_image:  "seedream-5-pro"    # 100 кредитов, только под смысловой кадр
    free_video:  "kling-25"          # 140 кредитов за 5 сек
    paid_video:  "bytedance-seedance-pro-2.0"

stock:
  # Same clip must not win every adjacent slot (0042 sparkle/NASA still loops).
  same_asset_max_slots: 1
  repeat_score_penalty: 0.12
  # Перебивка живёт 1.4 секунды ради того, чтобы в кадре что-то произошло.
  # Материал темнее этого показывает субтитр на пустоте: в 0047 так вышло
  # дважды, средняя яркость 17.7 и 20.1 из 255. Замер по базе: медиана
  # 55 % видимого кадра, у виновного клипа 16 %. Общего порога нет
  # намеренно — ночная эстетика канала в длинном кадре законна.
  interstitial_visible_min: 0.20
  # Freepik доступен по подписке Magnific и тем же ключом. Скачивание из
  # каталога кредитов не стоит, поэтому он первый в очереди источников.
  freepik_api_key_env: MAGNIFIC_API_KEY
  pexels_api_key_env: PEXELS_API_KEY
  pixabay_api_key_env: PIXABAY_API_KEY
  # NASA images-api и Internet Archive отдают материал анонимно — ключа нет
  # и не требуется, проверено запросом к images-api.nasa.gov.
  max_candidates_per_query: 14     # aggressive harvest (was 10; still within spirit of §7.2.3)
  queries_per_slot: 5              # MUST-016: 3–5 EN-запросов на слот, не больше
  target_pool_size: [50, 100]      # larger pool so avatar BGs + templates both get plates
  max_download_height: 1080        # §3.6.1: строго до 1080p, выше не брать
  # Запас кандидатов до платной критики: на слот нужно ≥ 1.3× годных, иначе
  # добор дешёвым поиском, не vision (MUST-018 / MUST-024).
  candidate_surplus: 1.3
  # Локальная база на слот: 6 клипов при same_asset_max_slots=1 оставляли
  # хвост ролика пустым — prefer-пины занимали первые слоты, остальные
  # видели только дубли. 24 — окно поиска; в candidates.json на слот
  # кладём local_keep_per_slot, иначе слот 0 сжигает все уникальные id.
  local_candidates_per_slot: 24
  local_keep_per_slot: 2
  # Вес принимаемого материала. Хранилище лежит в репозитории, и клип на 45 МБ
  # остаётся в истории git навсегда. Прогон 0047 положил 27 клипов на 369 МБ —
  # при том, что самый длинный слот ролика 6.4 сек, футаж всегда играет с нуля,
  # а звук стока отбрасывается на каждом пути подготовки кадра (-an).
  # Замер самого тяжёлого файла: 45.5 МБ / 65.6 с → 9.8 МБ при SSIM 0.980.
  keep_sec: 20                     # запас втрое к самому длинному слоту
  intake_crf: 23                   # 21 → 12.5 МБ, 23 → 9.8, 26 → 6.7 (SSIM 0.972)
  dedup_hamming_max: 8             # §7.2.5: порог 8/64
  video_probe_frames: [0.10, 0.50, 0.90]

libraries:
  sfx:     { max_items: 20,  frozen_when_full: true }
  # 15, а не 5: заказчик просил «штук десять» разновидностей подложки —
  # динамичную скрипку, тихое пианино, что-нибудь под грустный ролик.
  # Пять дронов такого не дают: у них нет ни нот, ни атаки.
  music:   { max_items: 15,  frozen_when_full: true }
  memes:   { max_items: 100, frozen_when_full: true, per_run_intake: 18 }
  footage: { max_items: null, freeze: false, eviction: lru }

storage:
  # Индекс футажа — в git. Сами файлы — S3 (REDSHIFT_S3_*), если секреты есть;
  # иначе артефакт Actions. Библиотека не обязана жить в git.
  #
  # backend: local | s3
  backend: local
  local_root: "assets/footage"
  # 400 МиБ локального кэша на раннере. Индекс — в git; сами файлы — S3
  # (REDSHIFT_S3_*), если секреты есть, иначе артефакт Actions.
  max_bytes: 419430400
  s3:
    bucket_env: REDSHIFT_S3_BUCKET
    endpoint_env: REDSHIFT_S3_ENDPOINT
    access_key_env: REDSHIFT_S3_ACCESS_KEY
    secret_key_env: REDSHIFT_S3_SECRET_KEY
    prefix: "footage/"

features:
  avatar_matting: true      # §7.7 киллсвитч
  background_vfx: true      # зависит от avatar_matting
  ab_versions: false         # одна версия монтажа; B не собирается
  auto_topic_search: false  # MUST-023: явный --article-url / --topic, автопоиск не включать
  vision_qc: true           # §11.2 смысловой QC по финальному ролику

limits:
  # 25–35 с — короткий формат (заказчик 22.09: пробные ролики на 30 с).
  duration_sec: [25, 70]
  avatar_share: [0.20, 0.40]  # TZ-0050: 20–40%, not 35–50%
  max_event_gap_sec: 2.5
  max_shot_sec: 5
  max_shot_sec_with_events: 7
  min_shot_sec: 1.5                 # §3.6.2
  memes_per_video: [0, 2]
  bg_vfx_per_video: 2               # §1.10: ≤2 раза за ролик
  bg_vfx_sec: [2.0, 5.0]            # §1.10: 2–5 сек
  # Потолок AI-футажа. Заказчик: ≤10 %. Преимущество всегда за настоящим
  # материалом — сток, новостная вырезка, скриншот, кадр из статьи. Генерация
  # закрывает только то, чего не нашлось, и останавливается на границе.
  # Заказчик 22.09: генерация допустима до 20 %, реальный материал ≥80 %.
  ai_footage_share_max: 0.20
  ai_footage_share_target: [0.0, 0.20]
  # §11.2.1: доля проб, где кадр не про речь. Выше — blocking, ролик не выдаётся.
  vision_mismatch_share_max: 0.10
  fullscreen_text_per_video: [2, 4] # §5.2
  highlight_per_video: [1, 3]       # §5.5
  split_share_max: 0.25             # §3.5 режим B ≤25%
  footage_block_share_max: 0.40     # §3.5 режим C ≤40% одним блоком
  first_avatar_appearance_sec: 6.0
  first_event_sec: 0.8
  hook_sec: 3.0
  cta_tail_sec: 2.0
  end_silence_ms: 300
  cut_share_min: 0.70               # §4.3: Cut ≥70% склеек
  dynamic_transition_min_gap_sec: 6.0
  sfx_min_gap_sec: 2.0              # §4.4: плотность SFX ≤ 1 / 2 сек
  # Режиссёрский ролик звучит на каждой склейке таймлайна (заказчик 25.09):
  # склейки там чаще двух секунд, и общий порог снял бы половину ударов.
  sfx_min_gap_director_sec: 0.7
  # §10.2: `sfx_min_gap_sec` разводит звуки во времени, но не мешает одному
  # и тому же wav прозвучать восемь раз за ролик — это слышно как петля.
  sfx_same_file_max: 3
  # §10.4: мёртвая пауза в первые три секунды. Предупреждающий гейт QC-28.
  first_seconds_gap_ms: 200
  first_seconds_window: 3.0
  # §6.3 R-4: расхождение первого и последнего кадра для шва лупа, биты dHash.
  loop_seam_dhash_max: 12
  # §6.2 R-3: кадр короче порога — мигание, а не бит.
  min_visual_beat_sec: 0.8
  quote_max_words: 15
  pillarbox_per_video: 2            # §3.6.5
  gradient_per_video: 1             # §3.3.5
  template_overlap_with_prev_max: 0.20  # QC-6/17
  ab_min_template_diff: 0           # A/B выключен: одна версия

speech:
  # Темп 0050 r62. Заказчик: «темп речи ускорь, чтобы именно там в быстром
  # темпе, та-та-та-та-та, новости». Скорость самой модели поднять больше
  # нечем — `elevenlabs.pace: 1.2` уже упирается в потолок параметра speed у
  # ElevenLabs. Значит плотность берётся паузами: режем от 110 мс (было 150) и
  # оставляем 55–85 мс (было 80–120). Пауза-удар перед ответом не тронута —
  # она динамику создаёт, а не отнимает.
  pause_threshold_ms: 110           # §4.2.2 паузы выше порога режем
  pause_target_ms: [55, 85]
  # Единственная пауза ролика, которую не режут, а держат: перед словом-ударом
  # в блоке-ответе (script_playbook.md §6). Остальные паузы отнимают динамику,
  # эта её создаёт — ответ звучит как ответ, а не как продолжение фразы.
  # 0 выключает приём.
  punch_hold_ms: 320
  breath_max_ms: 280
  silence_floor_db: -42.0
  min_word_ms: 250                  # §5.1 длительность слова 250–450 мс
  max_word_ms: 450

audio:
  # Shorts слушают с телефона: −14 LUFS (норма YouTube для длинных) там «еле
  # слышно, только на полной громкости» (заказчик 25.09, 0052). Короткие
  # ролики мастерят громче — −10 LUFS; пики держит лимитер (true_peak_max).
  voice_lufs: -10
  # broadcast — компрессор и лимитер с упреждением (ffmpeg) на голосе и шине:
  # без них речь с пик-фактором ~15 дБ до −10 LUFS не поднять, лимитер
  # опускал всю дорожку. classic — прежний подъём + потолок (для −14).
  master: broadcast
  # Уровень подложки задан долей от голоса, а не абсолютным LUFS.
  # Заказчик 25.09: «фоном, тихо, на 5–7 % громкости» — доля 0.05–0.07,
  # то есть на 23–26 дБ тише голоса (при −10 LUFS голоса ≈ −33…−36 LUFS).
  # История: 02.09 доля 0.07 при голосе −14 (бед ~−37 LUFS) была «не слышна»,
  # и её подняли до 0.28–0.32. Если на телефоне подложка пропадёт — поднимать
  # здесь, не в коде.
  music_voice_ratio: [0.05, 0.07]
  # Запасной коридор, если доля не задана.
  music_lufs: [-26, -24]
  ducking_db: -7
  true_peak_max: -1
  # Пики SFX сдвинуты на +4 дБ вслед за голосом (−14 → −10 LUFS): баланс
  # «еле слышно, но слышно, что дорого» задавался относительно речи.
  sfx_peak_dbfs: [-12, -8]
  sample_rate: 48000
  channels: 2

render:
  # hyperframes — основной движок: кадр описывается разметкой и собирается
  # headless Chrome. ffmpeg остаётся на нарезке, обрезке и сведении звука,
  # remotion — вспомогательный движок с той же схемой edit-плана.
  engine: hyperframes      # hyperframes | ffmpeg | remotion
  hyperframes_quality: high
  crf: 19                  # §3.1: CRF 18–20
  preset: "medium"
  video_codec: "libx264"
  profile: "high"
  pix_fmt: "yuv420p"
  audio_codec: "aac"
  audio_bitrate: "224k"
  color_primaries: "bt709"
  faststart: true
  ab_versions: ["A"]
  # TEMP: auto = Gemini image → Grok Imagine → ffmpeg @1s.
  # Publishable thumbs need a live image key (GEMINI_API_KEY while XAI empty).
  thumbnail_mode: "auto"
  thumbnail_time_sec: 1.0
  thumbnail_grok_params:
    aspect_ratio: "9:16"
    resolution: "2k"
    quality: "high"

news:
  # §7.5 — квоты бюджетируются и кэшируются
  quotas: { currents: 250, apitube: 100, gdelt: 100 }
  freshness_hours: 48
  dedup_title_fuzzy: 0.85
  keywords: [ai, space, quantum, biotech, "climate tech", neuro, longevity]

budget:
  max_cost_per_video_usd: 6.00
  hard_stop_on_exceed: true
  # Прайс для расчёта сметы до прогона (USD). Правится по фактическим тарифам.
  price:
    elevenlabs_per_1k_chars: 0.30
    heygen_per_second: 0.05
    gemini_per_image: 0.0004
    gemini_image: 0.04
    grok_per_image: 0.0060
    magnific_per_image: 0.08
    magnific_per_video_sec: 0.20

paths:
  work_dir: "work"
  output_dir: "output"
  templates_dir: "templates"
  assets_dir: "assets"
  cache_dir: "cache"
  logs_subdir: "logs"
  template_scenarios: "config/template_scenarios.json"

logging:
  level: INFO
  json: true
  redact_env_secrets: true    # §7.6: ни один ключ не попадает в логи/артефакты
