[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    slow: тесты, требующие рендера или длинных ffmpeg-вызовов
filterwarnings =
    ignore::DeprecationWarning
