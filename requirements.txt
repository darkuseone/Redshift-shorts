# REDSHIFT — зависимости пайплайна.
# Намеренно минимальны: тяжёлые обработки делает ffmpeg, композитинг — Pillow+numpy.
# Всё, что не перечислено здесь, реализовано в src/lib без внешних зависимостей
# (loudness BS.1770, pHash, парсер TTF, WAV I/O) — чтобы прогон был воспроизводим
# на голом CPU-раннере GitHub Actions.

numpy>=1.26,<3
Pillow>=10.2,<12
PyYAML>=6.0,<7
requests>=2.31,<3
jsonschema>=4.21,<5

# ffmpeg/ffprobe: если системного бинаря нет, берётся статический из этого пакета
imageio-ffmpeg>=0.4.9,<0.7

# dev
pytest>=8.0,<9
